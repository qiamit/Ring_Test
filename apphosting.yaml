# Firebase App Hosting — ring-manager-backend
# Environment variables are managed in Firebase Console (not in this file):
# https://console.firebase.google.com/project/ring-test-manager/apphosting
# → ring-manager-backend → Settings → Environment

runConfig:
  cpu: 1
  memoryMiB: 1024
  minInstances: 0
  maxInstances: 10
  concurrency: 80
